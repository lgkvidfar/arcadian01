import React from 'react';

const RenderUserListFromApi = () => {
    return (
        <div>
            {/* {isLoading && <div>api is loading...</div>}
      {isFetching && <div>api is fetching...</div>}
      {isSuccess && console.log('fetching todos was a success!')}
      {data && <div> {data.todos.map((user, index) => {
          return (
            <div key={index}>
                <p>users go here{index}</p>
            </div>
          );
        })}
      </div>}
      {error && <div>there was an errror in RenderTodoListFromApi</div>}
      {error && console.log(error)} */}
        </div>
    );
};

export default RenderUserListFromApi;
