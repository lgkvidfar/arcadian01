import React from 'react';
import Header from '../components/generic/Header';

const ProductPage = () => {
    return (
        <div>
            <Header text="list of products goes here" />
        </div>
    );
};

export default ProductPage;
