import React from 'react';

const PlayGround = () => {
    return (
        <div>
            <p>This is the playground page .</p>
            <button>set context</button> | <button>remove context</button>
        </div>
    );
};

export default PlayGround;
